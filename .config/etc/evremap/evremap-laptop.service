# /usr/lib/systemd/system/evremap-laptop.service
[Service]
WorkingDirectory=/
ExecStart=bash -c "/usr/bin/evremap remap /etc/evremap-laptop.toml -d 0"
Restart=always

[Install]
WantedBy=multi-user.target
